apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: mern-app
  labels:
    app: mysql
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None  # Headless service for StatefulSet DNS resolution
